console.log(
  [
    "Seed script is database-backed through app startup.",
    "Run `npm install` first, then open the app or import the same schema/catalog logic from lib/db.ts.",
    "This environment cannot install packages because npm registry lookups are blocked.",
  ].join("\n"),
);
